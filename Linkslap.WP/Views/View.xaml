<controls:PageBase
    x:Class="Linkslap.WP.Views.View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Linkslap.WP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Linkslap.WP.Controls"
    xmlns:viewModels="using:Linkslap.WP.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource BackgroundBrush}">

    <Page.DataContext>
        <viewModels:ViewLinkViewModel/>
    </Page.DataContext>
    
    <Grid Background="{ThemeResource BodyBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Style="{ThemeResource HeaderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Style="{ThemeResource HeaderImagePartial}" />
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Foo" Name="Title" Foreground="White" FontSize="20" TextWrapping="WrapWholeWords" Padding="15, 0, 10, 10"></TextBlock>
                    <TextBlock Text="Slapped at 7/23/2014 1:45 pm by myron to aaron/john/myron" Padding="15,0,10,0" Name="LinkInfo" Style="{ThemeResource ListItemInfoTextStyle}" />
                </StackPanel>
            </Grid>
            <Rectangle Height="20" Fill="#323232"></Rectangle>
        </StackPanel>
        <WebView Grid.Row="1" Name="WebView"></WebView>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="Gif Search" Icon="Zoom"/>
            <AppBarButton Label="Previous" Icon="Back"/>
            <AppBarButton Label="Next" Icon="Forward" />
        </CommandBar>
    </Page.BottomAppBar>
</controls:PageBase>
