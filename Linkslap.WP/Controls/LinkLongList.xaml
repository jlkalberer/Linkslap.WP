<UserControl
    x:Class="Linkslap.WP.Controls.LinkLongList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Linkslap.WP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <DataTemplate x:Key="LinksTemplate">
            <TextBlock Text="{Binding Comment}"/>
        </DataTemplate>
    </UserControl.Resources>

    <SemanticZoom Name="List">
        <!--<SemanticZoom.ZoomedOutView>
            <ListView Name="DateView" ItemTemplate="{StaticResource DateTemplate} "></ListView>
        </SemanticZoom.ZoomedOutView>-->
        <SemanticZoom.ZoomedInView>
            <ListView Name="ListView" ItemTemplate="{StaticResource LinksTemplate}" SelectionChanged="Selector_OnSelectionChanged">
            </ListView>
        </SemanticZoom.ZoomedInView>
    </SemanticZoom>
</UserControl>
