<UserControl
    x:Class="Linkslap.WP.Controls.AnimationImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Linkslap.WP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.Resources>
            <Storyboard x:Name="storyboard" RepeatBehavior="Forever"/>
        </Grid.Resources>
        <Image x:Name="image"/>
        <ProgressRing Name="ProgressRing" Canvas.ZIndex="2" IsActive="True"/>
        <TextBlock FontSize="40" x:Name="ErrorIcon" Text="&#xE106;" FontFamily="Segoe UI Symbol" Visibility="Collapsed" Foreground="Red" Canvas.ZIndex="2" VerticalAlignment="Center" HorizontalAlignment="Center" />
    </Grid>
</UserControl>
